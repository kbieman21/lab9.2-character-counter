## Reflection Questions
# How did you handle state updates when the text changed?
I used the useState function to handle the text change. On change I was listening to an even in the target and setText function updates the text.
# What considerations did you make when calculating reading time?
When I calculate reading time, I asked myself what is the average reading per minute? I took that number as a standard then I took the text inserted in my text area and divide it by the average standard to come up to the word read in minute.
# How did you ensure the UI remained responsive during rapid text input?
By using the useState function I ensured the UI remain responsive.
# What challenges did you face when implementing the statistics calculations?
When calculating, knowing the average reading time was the challenge.